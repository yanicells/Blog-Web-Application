<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Blogcells</title>
</head>

<body class="min-h-screen flex flex-col bg-gray-900">
    <%- include("partials/header.ejs") %>

    <div class="bg-gray-900 pb-16 pt-8 sm:pb-24 sm:pt-12">
        <div class="mx-auto max-w-7xl px-6 lg:px-8">
            <div class="mx-auto max-w-full lg:mx-0 text-center lg:text-left">
                <h1 class="text-5xl font-bold tracking-tight text-white sm:text-6xl lg:text-7xl">
                    From the Blog
                </h1>
                <p class="mt-8 text-xl leading-8 text-gray-300 max-w-2xl">
                    Discover insights, stories, and expert perspectives that matter. 
                    Join our community of writers and readers sharing knowledge.
                </p>
                <div class="mt-10 flex items-center justify-between gap-x-6">
                    <a href="/create"
                        class="group relative inline-flex items-center px-8 py-4 text-lg font-semibold text-white bg-blue-600 rounded-xl hover:bg-blue-700 transition-all duration-200 shadow-lg hover:shadow-xl hover:-translate-y-0.5">
                        <svg class="w-5 h-5 mr-2 transition-transform group-hover:scale-110" fill="none" stroke="currentColor"
                            viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                        </svg>
                        Create Post
                    </a>

                    <form method="POST" action="/search" class="w-full lg:w-96 pt-10 lg:pt-0">
                        <label for="default-search" class="mb-2 text-sm font-medium text-gray-900 sr-only dark:text-white">Search</label>
                        <div class="relative">
                            <div class="absolute inset-y-0 start-0 flex items-center ps-3 pointer-events-none">
                                <!-- SVG icon -->
                            </div>
                            <input type="search" name="query" id="default-search" class="block w-full p-4 ps-10 text-sm text-gray-900 border border-gray-300 rounded-lg bg-gray-50
                                      focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white
                                      dark:focus:ring-blue-500 dark:focus:border-blue-500" placeholder="Search Articles, Authors..."
                                required />
                            <button type="submit" 
                                class="text-white absolute end-2.5 bottom-2.5 bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none 
                                       focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
                                Search
                            </button>
                        </div>
                    </form>

                </div>
            </div>

            <div
                class="mx-auto mt-10 grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 border-t border-gray-700 pt-5 sm:mt-8 sm:pt-8 lg:mx-0 lg:max-w-none lg:grid-cols-3">

                <% if(locals.article){ %>
                    <% for(let i=0; i < article.length; i++){ %>

                <div class="max-w-sm bg-white border border-gray-200 rounded-lg shadow-sm dark:bg-gray-800 dark:border-gray-700">
                    <% if(article[i].image_url){ %>
                    <a href="/view/<%= i %>">
                        <img class="rounded-t-lg w-full object-cover" src="<%= article[i].image_url %>" alt="Cover image for <%= article[i].title %>" />
                    </a>
                    <% } %>
                    <div class="p-5">
                        <a href="/view/<%= i %>">
                            <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white"><%= article[i].title %></h5>
                        </a>
                        <p class="mb-3 font-normal text-gray-700 dark:text-gray-400"><%= article[i].body.split(' ').slice(0, 20).join(' ') + (article[i].body.split(' ').length > 20 ? '...' : '') %></p>
                        <div class="mb-3 text-xs text-gray-500 dark:text-gray-400">
                            <span>By <%= article[i].authorName %></span>
                            <span class="mx-1">•</span>
                            <span><%= new Date(article[i].created_at).toLocaleDateString('en-US', { 
                                year: 'numeric', 
                                month: 'short', 
                                day: 'numeric' 
                            }) %></span>
                        </div>
                        <a href="/view/<%= i %>"
                            class="inline-flex items-center px-3 py-2 text-sm font-medium text-center text-white bg-blue-700 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
                            Read more
                            <svg class="rtl:rotate-180 w-3.5 h-3.5 ms-2" aria-hidden="true" xmlns="http://www.w3.org/2000/svg"
                                fill="none" viewBox="0 0 14 10">
                                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M1 5h12m0 0L9 1m4 4L9 9" />
                            </svg>
                        </a>
                    </div>
                </div>
                <% } %>
                <% } %>

            </div>
        </div>
    </div>
</body>
<%- include("partials/footer.ejs") %>

</html>